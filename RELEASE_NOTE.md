# Mouse4 发布说明（Release Notes）

> 汇总各版本发布说明，最新改动在最上方。当前最新版本：**V107.0**。

---

## 🎯 V107.0 - 混合 DPI 逐屏抓取跨屏截图（最新）

本次更新彻底解决多显示器（不同分辨率 + 不同缩放比例 + 竖屏混排）下的跨屏截图问题：修复截图最左侧白乱码条、左右屏黑屏、工具栏在屏幕底部不显示三个问题。同时恢复双版本发布（单文件版 + 目录版）。

### ✨ 核心亮点

🖥️ **物理像素逐屏抓取（核心重构）**
对每块屏独立抓取（逐屏 BitBlt），再按物理偏移拼接成整块虚拟画布，物理像素级 1:1 对齐。彻底替代"单次整块虚拟桌面 BitBlt"——后者在混合 DPI（如 150% 主屏 + 100% 副屏）下会产生接缝缩放垃圾，表现为截图最左侧出现几像素宽、与副屏等高的白色乱码条。

📐 **混合 DPI 坐标体系修正**
实测发现 Qt 开启高 DPI 缩放后 `QScreen::geometry()` 返回"物理位置 + DIP 尺寸"的混合坐标，导致 DIP 画布拼接错位、左右屏黑屏。现恢复物理像素模式（禁用 Qt 缩放，保持 Per-Monitor V2 感知），`QScreen::geometry()` 与 mss 抓取完全一致，画布/窗口/鼠标/选区 1:1 对齐。

🧰 **工具栏定位重写**
候选位置依次为：选区下方 → 选区上方 → 选区内侧顶部，每个候选必须"完全在窗口内且不落在无显示器空隙带"。修复：选区贴近屏幕底部（或短屏下方为无显示器空隙带）时工具栏看不见的问题。

✂️ **选区自动钳制**
松手/保存时选区自动钳制到"显示器联合区域"，不会把屏间空隙（无显示器区域）截进截图。

📦 **双版本发布**
- 单文件版：`dist\Mouse4.exe`（约 58MB，便携）
- 目录版 ⭐：`dist\Mouse4\`（启动快，日常推荐）
- 同源构建（`mouse4.spec` / `mouse4_onedir.spec`），行为完全一致。

### 🐛 修复列表
- 修复 截图最左侧白乱码条（混合 DPI 下单次 BitBlt 接缝垃圾 → 逐屏抓取）
- 修复 目录版左右屏黑屏/错位（Qt 缩放开启后 geometry 混合坐标 → 恢复物理像素模式）
- 修复 选区在屏幕底部时工具栏不显示（工具栏定位重写 + 空隙带检测）
- 修复 跨屏选区把无显示器空隙带截进图（选区钳制到显示器联合区域）
- 新增 HDR 黑屏诊断（日志 `WARNING ... need DXGI` 提示某屏为 HDR，当前暂不支持）

---

## 🎯 V103.0 - 稳定性持续加固版

本次更新修复了两个运行时崩溃：截图白屏崩溃（快捷键触发后屏幕全白然后程序退出）和箭头标注类型错误。同时增加了 30 天日志自动裁剪功能。

### ✨ 核心亮点

📸 **截图白屏崩溃已修复**
按下截图快捷键后屏幕全白然后程序退出的问题已解决。根因是 `SnippingWindow` 构造函数中 `showFullScreen()` 在所有属性初始化之前调用，Qt 立即触发 `paintEvent` 但 `full_screenshot` 属性尚不存在。现已将所有属性初始化移到 `showFullScreen()` 之前。

✏️ **箭头标注类型错误已修复**
`draw_arrow` 中 `end` 是 `QPoint` 但与 `QPointF` 进行减法运算时报 `TypeError`。修复为函数入口统一转换为 `QPointF`。

🗑️ **30 天日志自动裁剪**
`debug.log` 写入时自动清理超过 30 天的记录，`[FATAL CRASH]` 行永久保留。每天最多执行一次，无性能开销。解决 AppData 目录日志长期累积问题。

### 🐛 修复列表
- 修复 截图快捷键后白屏崩溃（`full_screenshot` 初始化时序）
- 修复 箭头标注 `QPoint` 与 `QPointF` 类型错误崩溃
- 修复 日志文件无限增长（30 天自动裁剪）

---

## 🚀 V90.0 - 终极纯净版：架构重塑与全域稳定 (The Ultimate Clean)

经过多轮版本迭代与技术验证，我们带来了 Mouse4 史上架构最精简、运行最稳定的版本 V90.0！

本次更新是一次彻底的"架构净化"——移除了所有实验性的冗余桥接层和延迟加载策略，回归最纯粹的单文件运行哲学。同时集成了此前多个版本的成熟修复成果，将稳定性推向新高度。从底层热键注册到上层 UI 绘制，每一个环节都经过了重新审视与加固。

### ✨ 核心亮点 (Highlights)

🛡️ **全域崩溃拦截网 (Black Box)**
不再容忍静默死亡。V90.0 挂载了 `sys.excepthook` + `threading.excepthook` 双层异常拦截器。无论是主线程还是后台守护线程发生未处理崩溃，程序都会自动弹出可视化报警对话框，精准定位错误并写入日志。每一次崩溃，都有迹可循。

⚡ **原生热键 + 强力重启双重保障**
采用 Windows 官方 `RegisterHotKey` API 注册全局热键 Ctrl+1，热键直接挂载于内核消息队列，免疫睡眠唤醒。同时看门狗 (Watchdog) 每 5 秒侦测时间跳变，一旦检测到深度休眠后的系统苏醒，立即执行 Hard Restart 启动全新进程，确保热键 100% 抢占成功。

🖱️ **"不死图腾" 鼠标监听**
为资源管理器"双击空白处返回"功能移除了脆弱的局部导入模式，将 `pynput` 提前至模块最顶层加载并锁定内存地址。无论系统休眠多久，唤醒后监听线程依然健壮如初。

📋 **纯净剪贴板方案**
移除了冗余的 QBuffer/QIODevice 桥接层，回归 PIL 直写方案。在内存中精准生成标准 DIB 格式并剥离 BMP 文件头，截图粘贴 100% 成功率，干净利落。

### 🐛 错误修复 (Bug Fixes)
- 修复 电脑睡眠唤醒后 `pynput` 报 `ImportError` 导致监听线程崩溃的问题
- 修复 截图选区靠近屏幕底部时，工具栏溢出不可见的问题（智能反弹至选区上方）
- 修复 托盘图标运行数小时后随机消失的问题（全局强引用防止 GC 回收）
- 修复 DPI 手动设置与 `uiautomation` 的时序冲突告警
- 移除 `Failed to remove temporary directory` 和 `_imaging` 加载失败的报错弹窗

### 🛠️ 技术细节 (Under the Hood)
- **架构重塑**：彻底移除了实验性的"延迟加载 (Lazy Load)"，恢复所有重型库的全局前置导入，确保程序一旦启动运行环境就 100% 稳固。废弃了冗余的 QBuffer 内存中转层，减少不必要的对象生命周期管理。
- **告别系统垃圾**：恢复最标准的单文件 (-F) 运行模式。用完即走，绝不在用户的 AppData 中留下程序解压残骸（仅保留轻量级 `config.json` 和运行日志）。
- **UI 智能适配**：工具栏坐标计算逻辑重写，自动检测屏幕边界并智能选择弹出方向，从此告别截图工具栏"跑出屏幕"的尴尬。

💡 **升级建议**：强烈建议所有 V83 及更早版本的用户下载替换此版本。V90.0 集成了此前多个版本的成熟修复成果，同时移除了实验性包袱，是当前最值得信赖的版本。
